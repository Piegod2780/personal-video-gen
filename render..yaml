services:
  - type: web
    name: personal-video-gen
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run sora_app.py --server.port $PORT --server.address 0.0.0.0
